import { ModalImagesPipe } from './modal-images.pipe';

describe('ModalImagesPipe', () => {
  it('create an instance', () => {
    const pipe = new ModalImagesPipe();
    expect(pipe).toBeTruthy();
  });
});
